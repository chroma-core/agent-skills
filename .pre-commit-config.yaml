repos:
  - repo: local
    hooks:
      - id: build-and-validate
        name: Build and validate
        entry: bun run validate
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
